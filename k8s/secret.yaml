apiVersion: v1
kind: Secret
metadata:
  name: heroes-secrets
type: Opaque
data:
  # These are example values - in a real environment use real secrets
  # The values are base64 encoded
  # echo -n "your-secret-value" | base64
  MONGODB_USER: YWRtaW4=          # admin
  MONGODB_PASSWORD: cGFzc3dvcmQ=   # password