apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: heroes-api-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: heroes-api.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: heroes-api-service
            port:
              number: 80